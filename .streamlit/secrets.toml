# Streamlit secrets file - put your real values here (DO NOT commit real secrets)
# Use Streamlit Cloud's Advanced > Secrets to set these, or create this file locally at .streamlit/secrets.toml

# API key for Groq LLM (optional). If absent, deterministic fallback is used.
GROQ_API_KEY = "your_groq_api_key_here"

# Backend URL for external server (optional when not using embedded backend)
BACKEND_URL = "http://localhost:8001"

# If set to "1" or "true", the app will initialize and use the embedded recommendation engine
EMBED_BACKEND = "1"
